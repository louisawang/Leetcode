class Solution:
    # @param A, a list of integer
    # @return an integer
    def singleNumber(self, A):
        n=len(A)
        result=A[0]
        for i in range(1,n):
            result^=A[i]
            
        return result
        
